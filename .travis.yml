language: python

python:
- '3.5'

matrix:
  include:
    - python: 3.8-dev
      dist: xenial

install:
- pip install -r test_requirements.txt

script:
- flake8
- python -m unittest test.cfgs_test

# Don't compile "experimental" branches starting with x-, y-, or z-.
branches:
  except:
    - /^x-.*$/
    - /^y-.*$/
    - /^z-.*$/
